# Extend logging

See example tracking ideas,. 

## To record
- Project/instance
- User / reason
- Model and Provider
- full prompt (OK that gets big). Also this doesn't change, but I want to experiment.
- maybe githash of system


## Vis stuff should be supported.

## Goal: compare same query across different models

Pretty easy to do a different query


# Basic logger is too basic

Just shows the last 10 queries, Probably want the whole dataset in an ag-grid.

